#!/bin/sh
set -eu

case "$(uname -m)" in
  x86_64) arch=amd64 ;;
  aarch64) arch=arm64 ;;
  *) echo "magpie: unsupported architecture: $(uname -m)" >&2; exit 1 ;;
esac
exec "/app/extra/magpie-linux-$arch" "$@"
