<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.block.Goose</id>
  <name>goose</name>
  <summary>Extensible AI agent that installs, executes, edits and tests</summary>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>Apache-2.0</project_license>
  <developer id="io.github.block">
    <name>AAIF (Agentic AI Foundation)</name>
  </developer>
  <url type="homepage">https://goose-docs.ai/</url>
  <url type="bugtracker">https://github.com/aaif-goose/goose/issues</url>
  <url type="help">https://goose-docs.ai/docs/quickstart</url>
  <url type="vcs-browser">https://github.com/aaif-goose/goose</url>
  <description>
    <p>This is a community package of goose, maintained independently and unaffiliated with goose or the Agentic AI Foundation. It repackages the official upstream Linux build unmodified.</p>
    <p>goose is an open-source AI agent that goes past code suggestions: it runs on your machine, uses any model you point it at, and carries out whole tasks — installing dependencies, running commands, editing files and testing the result.</p>
    <ul>
      <li>Any model: the hosted providers, an OpenAI-compatible endpoint, or a local Ollama.</li>
      <li>Extensions over the Model Context Protocol, plus built-in developer, computer-control and memory tools.</li>
      <li>Recipes and scheduled runs for work you repeat.</li>
      <li>Session transcripts you can revisit, and a CLI that shares the same engine as the desktop app.</li>
    </ul>
    <p>MCP toolchain: extensions started through the bundled node, npx and uvx shims provision themselves with hermit on first use, fetching hermit, its package registry and a Node toolchain into ~/.config/goose. That step clones a git repository, and the runtime carries no git, so this package forwards git to the one installed on your host. Two consequences worth knowing before you install it:</p>
    <ul>
      <li>Git must be installed on the host, or MCP extensions cannot provision. Everything else works without it.</li>
      <li>The package holds the Flatpak portal permission that forwarding needs, which means a command this app runs can reach outside the sandbox. goose is an agent that runs commands on your behalf, so treat that as a deliberate trade rather than an incidental one.</li>
    </ul>
    <p>~/.config/goose is granted as a real directory on your machine — the shims put the toolchain there, and the host git has to write to the same path the sandbox reads.</p>
    <p>Sandbox otherwise: display, GPU, network, notifications and the Secret Service for API-key storage, and no further filesystem grant. Sessions live in the app's own data directory, and the agent's file and shell tools act inside the sandbox. To let it work on real projects, grant the directories you want it to have:</p>
    <p>flatpak override --user --filesystem=~/Projects io.github.block.Goose</p>
    <p>Replace the path with your own, repeat the flag for more directories, and undo everything with:</p>
    <p>flatpak override --user --reset io.github.block.Goose</p>
  </description>
  <content_rating type="oars-1.1"/>
  <categories>
    <category>Development</category>
  </categories>
  <keywords>
    <keyword>ai</keyword>
    <keyword>agent</keyword>
    <keyword>assistant</keyword>
    <keyword>llm</keyword>
    <keyword>mcp</keyword>
  </keywords>
  <branding>
    <color type="primary">#1a1a1a</color>
  </branding>
  <recommends>
    <display_length compare="ge">800</display_length>
    <control>keyboard</control>
    <control>pointing</control>
  </recommends>
  <provides>
    <binary>goose</binary>
    <mediatype>x-scheme-handler/goose</mediatype>
  </provides>
  <launchable type="desktop-id">io.github.block.Goose.desktop</launchable>
  <screenshots>
    <screenshot type="default">
      <image>https://raw.githubusercontent.com/aaif-goose/goose/main/documentation/static/img/mcp-ui-shot.png</image>
      <caption>An agent session using an MCP extension</caption>
    </screenshot>
  </screenshots>
  <releases>
    <release version="1.47.0" date="2026-08-21">
      <description></description>
    </release>
    <release version="1.46.0" date="2026-08-12">
      <description></description>
    </release>
    <release version="1.45.0" date="2026-07-29">
      <url>https://github.com/aaif-goose/goose/releases/tag/v1.45.0</url>
    </release>
  </releases>
</component>
